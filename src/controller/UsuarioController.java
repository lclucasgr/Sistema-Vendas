package controller;

import dao.DaoUsuarios;
import java.util.ArrayList;
import model.Usuarios;

public class UsuarioController {

    /*

    private DaoUsuarios daoUsuarios = new DaoUsuarios();

    public int salvarUsuarioController(Usuarios usuario) {
        return this.daoUsuarios.salvarUsuario(usuario);
    }

    public boolean excluirUsuarioController(int id) {
        return this.daoUsuarios.excluirUsuario(id);
    }

    public boolean alterarUsuarioController(Usuarios usuario) {
        return this.daoUsuarios.alterarUsuario(usuario);
    }

    public Usuarios getUsuariosByIdController(int id) {
        return this.daoUsuarios.getUsuariosById(id);
    }

    public ArrayList<Usuarios> getUsuarios() {
        return this.daoUsuarios.getUsuarios();
    }
     */
}
